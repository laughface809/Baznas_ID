import React from 'react'
import { Box } from '@mui/material'

const Slider = ({ children }) => {
    return (
        <Box mb={8}>
            {children}
        </Box>
    )
}

export default Slider